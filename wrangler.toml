name = "devvibes-landing"
pages_build_output_dir = "dist"
compatibility_date = "2024-01-15"

[vars]
# Public env vars (non-secret) can go here.
# Secret vars (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY) must be set via
# `wrangler pages secret put` or in the Cloudflare dashboard:
# Workers & Pages → devvibes-landing → Settings → Environment Variables
